- [x] Implement GLES 2.0 bridge functions in `jni_bridge_arm64.cpp`
- [x] Register GLES 2.0 handlers in `jni_bridge_arm64.cpp`
- [x] Implement dynamic shader replacement in `glShaderSource`
- [x] Implement dynamic uniform injection in `glUseProgram`
- [x] Implement dynamic boot-time component interface resolver
- [x] Implement dynamic boot-time function resolver for all global engine functions (eliminating all hardcoded function offsets)
- [x] Fix swapped PVR texture dimensions (width/height) in JNI bridge
- [x] Correct all ARM64 method calls in `sre_scene_update.c` and `sre_mini_api.c` to use dynamic function pointers
- [x] Bind walk/run speed and jump height cheats directly to CharController fields
- [x] Correct text input delegate offset in `sre_gui_native.c` and comment out unused vtable overrides in `sre_init.c`
- [x] Resolve the virtual heap memory leak in `jni_bridge_arm64.cpp` by implementing memory recycling, and set threshold to 2GB
- [x] Fix potential out-of-bounds reads in `bridge_glShaderSource` for non-null-terminated strings
- [ ] Verify build & integration (via user validation)
